<h1>Make A New VendorSweet</h1>
<%= form_for @vendor_sweet do |f| %>
<%= f.label :vendor_id %>
<%= f.collection_select :vendor_id, @vendors, :id, :name %><br>
<%= f.label :sweet_id %>
<%= f.collection_select :sweet_id, @sweets, :id, :name %><br>
<%= f.submit %>
<% end %>
